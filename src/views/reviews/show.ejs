<% if(movie.reviews) { %>
  <div style="width: 50%">
    <hr>
    
    <h4>Review:</h4>

    <p><%= movie.reviews.body %></p>

    <hr>

    <h4>Rating:</h4>

    <p><%= movie.reviews.rating %></p>

    <hr>

    <small class="card-subtitle mb-2 text-muted">
      created on <%= movie.reviews.createdAt %>
    </small>

    <% if(currentUser && currentUser.id === movie.reviews.userId) { %>
      <a href="/users/<%= movie.userId %>/movies/<%= movie.id %>/reviews/<%= movie.reviews.id %>/edit" class="btn btn btn-sm btn-outline-primary">Edit</a>

      <form style="display:initial"
            action="/users/<%= movie.userId %>/movies/<%= movie.id %>/reviews/<%= movie.reviews.id %>/destroy"
            method="post">
        <button type="submit" class="btn btn btn-sm btn-outline-danger">Delete</button>
      </form>
      
    <% } %>
  
  </div>
<% } %>

